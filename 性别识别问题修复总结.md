

---

## 性别识别问题修复总结

### 一、问题现象

用户多次告知寻慧“我是女的”，但寻慧仍然称呼用户为“哥哥”、“老哥”，且询问“我的性别是什么”时回答错误。

---

### 二、根因分析（两个独立问题叠加）

| 问题 | 位置 | 原因 |
|------|------|------|
| **执行顺序错误** | `talkToOllama` 函数 | `_analyzeAndUpdateUserProfile` 放在预设回复匹配之后，导致“我是女的”被预设回复系统拦截，性别识别代码从未执行 |
| **提示词指令不够强** | `_buildSystemPrompt` 函数 | 只建议 AI “称呼用户为姐姐”，没有断言“用户就是女性”，AI 凭训练数据中的性别刻板印象自行发挥 |

---

### 三、改动的具体内容

#### 改动 1：调整 `talkToOllama` 函数执行顺序（`chat.js`）

**修改前（错误）：**

```javascript
// 预设回复
if (!skipLearning) {
    _analyzeAndUpdateUserProfile(userMessage);  // ← 被关在这里
    const canned = _matchCannedReply(userMessage, { skipDiary, onEnd });
    if (canned) return;  // ← 预设回复匹配成功就直接返回，跳过了上一行
}
```

**修改后（正确）：**

```javascript
// ===== 用户画像分析：必须在预设回复之前，且不受 skipLearning 限制 =====
_analyzeAndUpdateUserProfile(userMessage);

// 预设回复
if (!skipLearning) {
    const canned = _matchCannedReply(userMessage, { skipDiary, onEnd });
    if (canned) return;
}
```

**效果**：每次用户说话，`_analyzeAndUpdateUserProfile` 都会在第一时间执行，不再被预设回复或任何拦截逻辑跳过。

---

#### 改动 2：强化 `_buildSystemPrompt` 函数中的性别指令（`chat.js`）

**修改前（弱指令）：**

```javascript
let genderHint = profile.gender === 'female'
  ? '你称呼用户为"姐姐"或"喂"（心情好时偶尔会叫"笨蛋姐姐"）。'
  : profile.gender === 'male'
    ? '你称呼用户为"哥哥"或"喂"（心情好时偶尔会叫"笨蛋哥哥"）。'
    : '你称呼用户为"喂"、"笨蛋"或"说你呢"，语气像个没大没小的损友。';
```

**修改后（强指令）：**

```javascript
let genderHint = '';
if (profile.gender === 'female') {
  genderHint = '【事实】用户是女性，这是确定的。你必须称呼用户为"姐姐"或"喂"（心情好时偶尔叫"笨蛋姐姐"）。严禁使用"哥哥"、"老哥"等任何男性称呼。';
} else if (profile.gender === 'male') {
  genderHint = '【事实】用户是男性，这是确定的。你必须称呼用户为"哥哥"或"喂"（心情好时偶尔叫"笨蛋哥哥"）。严禁使用"姐姐"等任何女性称呼。';
} else {
  genderHint = '用户没有设定性别，你必须用"喂"、"笨蛋"等中性称呼，严禁猜测性别。';
}
```

**关键变化**：
- 加了 `【事实】` 标签，AI 对这种绝对化表述更敏感
- 加了 `这是确定的`，消除 AI 的猜测空间
- 加了 `严禁使用` 的禁令，堵死错误称呼的可能

---

#### 改动 3：手动修复已存在的错误数据

**原因**：之前的 bug 导致 `userProfile.gender` 被持久化为 `"male"`。代码修复后不会自动改回。

**操作**：打开 `C:\Users\HP\.girlpet_learning.json`，将 `"gender":"male"` 改为 `"gender":"female"`，保存，重启应用。

---

### 四、修复后的完整流程

用户说话 → `_analyzeAndUpdateUserProfile`（第一时间更新画像）→ `_interceptBuiltinCommands`（检查是否内置指令）→ `_matchCannedReply`（匹配预设回复）→ `_buildSystemPrompt`（用最新画像构建强性别指令）→ `_streamOllamaResponse`（AI 生成回复，严格遵循性别指令）

---

### 五、改动涉及的文件

| 文件 | 改动行数 | 改动内容 |
|------|----------|----------|
| `src/renderer/chat.js` | 第 320-326 行 | 将 `_analyzeAndUpdateUserProfile` 从 `if (!skipLearning)` 内部提到外部 |
| `src/renderer/chat.js` | 第 234-239 行 | 强化 `genderHint` 的表述，加 `【事实】`、`严禁` 等关键词 |
| `C:\Users\HP\.girlpet_learning.json` | 1 处 | 手动改 `"gender":"male"` → `"gender":"female"` |

---

这就是刚才所有改动的完整总结。一次修了两个问题：一个让她的耳朵不再选择性失聪，一个把“用户是女性”这个事实焊进了她的核心代码里。