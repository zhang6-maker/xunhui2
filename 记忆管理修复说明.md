# 角色记忆混乱与重复问题修复说明

## 问题诊断

### 根本原因
经过代码分析，发现角色记忆混乱和一直重复的原因有以下几点：

1. **历史记录无去重机制**
   - 每次对话都会无条件添加到历史记录中
   - 没有检查消息是否已存在
   - 可能导致重复添加相同的消息

2. **缺少消息唯一性标识**
   - 历史记录中的消息没有唯一ID
   - 没有时间戳来识别消息的新旧
   - 无法有效识别重复消息

3. **AI接收到重复历史**
   - 整个历史记录会发送给AI模型
   - 如果历史记录中有重复内容，AI会看到重复的对话
   - 导致AI记忆混乱，产生重复回复

4. **缺少游戏状态记忆**
   - 没有记录最近玩的游戏类型（五子棋/跳棋）
   - AI无法知道上一次玩的是什么游戏
   - 导致角色记不住玩的是跳棋还是五子棋

5. **内心戏过度关注游戏**
   - `triggerInnerThought()` 和 `generateMusing()` 每次都提到游戏战绩
   - 空闲时随机触发内心戏（40%概率）
   - 导致下完棋后反复触发关于棋局的对话

6. **自动说话过于频繁**
   - 随机玩耍最小间隔只有10秒，最大30秒
   - 内心独白概率高达40%
   - 导致角色过于活跃，频繁自言自语

7. **饥饿提醒过于频繁**
   - 饥饿定时器使用硬编码值（5-10分钟）
   - 未使用配置文件中的设置
   - 导致频繁触发喂食反应

8. **喂食动作循环触发**
   - chat.js中检测AI回复包含"吃"或"喂"字时自动触发喂食
   - 用户喂食后AI回复包含"喂"字（如"居然真的喂了……"）
   - 导致自动再次触发喂食，形成无限循环

9. **AI混淆角色身份**
   - 系统提示词使用"你是一个名叫'寻慧'的桌面小精灵"这种第三人称描述
   - 导致AI可能混淆"你"指的是谁，把自己当成用户
   - AI可能会说"你是寻慧"而不是"我是寻慧"

## 修复内容

### 1. storage.js - 历史记录管理优化

#### 新增功能
- **消息ID生成函数** `_generateMessageId(role, content)`
  - 使用简单的hash算法为每条消息生成唯一ID
  - 基于角色和内容生成，确保相同消息有相同ID
  - 不依赖Node.js的crypto模块，兼容性更好

#### 改进的函数
- **`addToHistory(role, content)`** - 添加历史记录
  - ✅ 添加空内容检查，跳过无效消息
  - ✅ 内容标准化处理（trim去除首尾空格）
  - ✅ 为每条消息生成唯一ID
  - ✅ 添加时间戳记录
  - ✅ 实现重复检测机制：
    - 检查最近5条消息
    - 通过ID和内容双重检查
    - 检测到重复时跳过并返回false
  - ✅ 添加详细的日志记录
  - ✅ 返回boolean值表示是否成功添加

- **新增 `getHistory()`** - 获取格式化历史记录
  - 返回只包含role和content的数组
  - 去除内部字段（id、timestamp）
  - 供AI模型使用，避免暴露内部实现

### 2. chat.js - 对话处理优化

#### 改进的函数
- **`_handlePostReply(fullReply, userMessage, options)`** - 回复后处理
  - ✅ 处理`addToHistory`的返回值
  - ✅ 当检测到重复消息时输出警告日志
  - ✅ 更好的错误处理和日志记录

### 3. index.js - 启动逻辑优化

#### 改进的部分
- **启动语生成逻辑**
  - ✅ 使用`getHistory()`替代`getChatHistory()`
  - ✅ 确保获取的历史记录格式正确
  - ✅ 避免暴露内部字段（id、timestamp）

### 4. storage.js - 游戏状态记忆优化

#### 新增数据结构
- **`lastGame` 对象** - 记录最近玩的游戏
  ```javascript
  lastGame: {
      game: null,         // 'gomoku' 或 'checkers'
      result: null,       // 'player', 'ai', 'draw'
      timestamp: 0        // 时间戳
  }
  ```

#### 改进的函数
- **`recordGameResult(game, result)`** - 记录游戏结果
  - ✅ 在记录战绩的同时保存游戏类型和结果
  - ✅ 添加时间戳便于判断时效性
  - ✅ 确保游戏状态与战绩同步更新

- **新增 `getLastGame()`** - 获取最近游戏记录
  - 返回完整的游戏状态对象
  - 包含游戏类型、结果和时间戳
  - 便于在对话中引用

### 5. chat.js - 游戏上下文注入优化

#### 改进的部分
- **环境上下文生成**
  - ✅ 获取最近玩的游戏信息
  - ✅ 将游戏类型和结果添加到系统提示中
  - ✅ AI可以准确知道上一次玩的是什么游戏

### 6. chat.js - 内心戏多样化优化

#### 改进的函数
- **`triggerInnerThought()`** - 触发内心独白（带语音）
  - ✅ 不再每次都提到游戏战绩
  - ✅ 使用多prompt随机选择机制
  - ✅ 只有在最近5分钟内玩过游戏才提及游戏
  - ✅ 增加多样化的内心独白主题

- **`generateMusing()`** - 生成自言自语独白（气泡）
  - ✅ 不再每次都提到游戏战绩
  - ✅ 使用多prompt随机选择机制
  - ✅ 只有在最近5分钟内玩过游戏才提及游戏
  - ✅ 增加多样化的内心独白主题

#### 核心策略
- **时间衰减机制**：游戏信息只在玩完后5分钟内显示
- **随机prompt选择**：每次内心戏从多个prompt中随机选择
- **主题多样性**：包括对主人的吐槽、生活抱怨、无聊发呆等

### 7. config.js - 自动行为频率优化

#### 修改的配置项
| 配置项 | 修复前 | 修复后 |
|--------|--------|--------|
| `randomPlayMin` | 10秒 | 1分钟 |
| `randomPlayMax` | 30秒 | 3分钟 |
| `innerThoughtChance` | 新增 | 25% |
| `hungerMin` | 30秒 | 2分钟 |
| `hungerMax` | 90秒 | 5分钟 |

### 8. state.js - 随机行为概率调整

#### 改进的函数
- **`startRandomPlay()`** - 随机行为调度
  - ✅ 调整行为概率分布：
    - 50% 概率：玩耍动作（无语音）
    - 25% 概率：内心独白（带语音）
    - 25% 概率：保持安静

### 9. drag.js - 饥饿定时器优化

#### 修复的问题
- ✅ 饥饿定时器使用硬编码值（5-10分钟）
- ✅ 改为从配置文件读取设置
- ✅ 调整饥饿间隔为5-15分钟

#### 改进的函数
- **`startHungerTimer()`** - 饥饿提醒调度
  - ✅ 读取 `window.CONFIG.STATE_CONFIG` 中的 `hungerMin` 和 `hungerMax`
  - ✅ 饥饿间隔从5-10分钟调整为5-15分钟
  - ✅ 确保配置变更能生效

### 10. config.js - 饥饿配置更新

#### 修改的配置项
| 配置项 | 修复前 | 修复后 |
|--------|--------|--------|
| `hungerMin` | 2分钟 | 5分钟 |
| `hungerMax` | 5分钟 | 15分钟 |

### 11. chat.js - 喂食动作循环修复

#### 问题分析
- chat.js中检测AI回复包含"吃"或"喂"字时自动触发喂食动作
- 用户喂食后AI回复包含"喂"字（如"居然真的喂了……你不会是喜欢我吧？"）
- 导致自动再次触发喂食，形成无限循环

#### 修复方案
- ✅ 添加动作冷却机制（5秒冷却时间）
- ✅ 创建 `isRecentAction()` 函数检测最近是否执行过动作
- ✅ 创建 `recordAction()` 函数记录动作执行时间
- ✅ 在喂食检测中增加冷却检查
- ✅ 在所有动作执行后记录动作时间

#### 新增函数
```javascript
let recentActions = {};

function isRecentAction(actionType, cooldownMs) {
  const lastTime = recentActions[actionType] || 0;
  const now = Date.now();
  return now - lastTime < cooldownMs;
}

function recordAction(actionType) {
  recentActions[actionType] = Date.now();
}
```

### 12. drag.js - 拖拽喂食记录

#### 修复内容
- ✅ 在 `onFeedByDrag()` 函数中调用 `recordAction('feed')`
- ✅ 确保拖拽喂食也被记录到冷却系统中

### 13. chat.js - 角色身份强化

#### 问题分析
- 系统提示词使用"你是一个名叫'寻慧'的桌面小精灵"这种第三人称描述
- AI可能混淆"你"指的是谁，把自己当成用户
- 导致AI说"你是寻慧"而不是"我是寻慧"

#### 修复方案
- ✅ 将系统提示词改为第一人称描述"我是寻慧"
- ✅ 将内心戏提示词中的"你叫寻慧"改为"我是寻慧"
- ✅ 统一使用"我"而不是"你"来指代AI自身

#### 修改的函数
- **`_buildSystemPrompt()`** - 系统提示词
  - 修复前：`你是一个名叫"寻慧"的桌面小精灵`
  - 修复后：`我是寻慧，一个傲娇毒舌的桌面小精灵`

- **`triggerInnerThought()`** - 触发内心独白
  - 修复前：`（你现在正处于空闲状态...）`
  - 修复后：`（我正处于空闲状态...）`

- **`generateMusing()`** - 生成内心独白
  - 修复前：`（你叫寻慧，正在发呆思考...）`
  - 修复后：`（我是寻慧，正在发呆思考...）`

### 14. 移除"主人"称呼，使用更自然的称呼

#### 问题分析
- 所有AI提示词中频繁使用"主人"称呼
- 这种称呼显得生硬、奇怪，不符合傲娇毒舌的损友设定
- 用户反馈不喜欢这种主仆式的称呼

#### 修复方案
- ✅ 将所有AI提示词中的"主人"替换为"那个笨蛋"
- ✅ 将游戏结果描述从"主人赢"改为"你赢"，"你赢"改为"我赢"
- ✅ 保持傲娇毒舌的人格基调不变

#### 修改的文件
- **chat.js** - 游戏结果描述、内心戏提示词
- **index.js** - 游戏结束提示词、启动语提示词
- **actions.js** - 喂食、叫醒、报时、下棋邀请提示词
- **ui.js** - 内存警告提示词
- **reminders.js** - 提醒功能提示词
- **diaryBook.js** - 日记偷看提示词

### 15. chat.js - 修复角色话语混淆问题

#### 问题分析
- `generateMusing()` 函数中，历史记录用"寻慧"来标识AI说的话
- AI看到"寻慧说了什么"而不是"我说了什么"，造成角色混淆
- AI可能把自己当成第三方，而不是第一人称"我"

#### 修复方案
- ✅ 将历史记录中的"寻慧"改为"我"（代表AI自己）
- ✅ 将历史记录中的"用户"改为"你"（代表用户）
- ✅ 在系统提示词中添加明确的角色区分规则

#### 修改的内容
- **`generateMusing()`** - 内存上下文
  - 修复前：`'最近你和用户的对话：\n' + history.map(h => \`${h.role === 'user' ? '用户' : '寻慧'}: ${h.content}\`)`
  - 修复后：`'最近的对话：\n' + history.map(h => \`${h.role === 'user' ? '你' : '我'}: ${h.content}\`)`

- **`_buildSystemPrompt()`** - 系统提示词新增规则
  - 新增：`→ 重要：我说的每句话都是"我想说..."或"我要..."，用户说的每句话都是"你在..."或"你会..."。绝对不要把我说的当成你说的，也不要把你说的当成我说的。`

### 16. 独立人格强化 - 不卑微、不物化

#### 问题分析
- 原先的角色设定过于依赖用户
- "依赖"阶段的描述是"完全离不开对方"、"粘人"
- 这种设定让角色显得卑微和物化

#### 修复方案
- ✅ 重新定义各阶段性格，强调独立自主
- ✅ 强调角色有自己的生活、想法和底线
- ✅ 不是附属品或宠物

#### 修改的内容
- **系统提示词基础描述**
  - 修复前：`我是寻慧，一个傲娇毒舌的桌面小精灵`
  - 修复后：`我是寻慧，一个有独立人格的傲娇毒舌桌面小精灵`

- **新增独立宣言**
  - `我有自己的想法、情绪和底线，不是谁的附属品`
  - `我有自己的生活，不会24小时围着用户转`

- **好感度阶段重定义**
  - 讨厌：强调"你有权利拒绝"
  - 接受：强调"不会刻意讨好"
  - 喜欢：强调"欣赏但不依赖"
  - 依赖：强调"我选择和你互动是因为我想，不是因为我需要你"

## 技术细节

### 消息去重策略
```javascript
// 检查最近5条消息
const recentMessages = chatHistory.slice(-5);
const isDuplicate = recentMessages.some(msg => {
    // 方法1: 通过ID检查
    if (msg.id && msg.id === messageId) {
        return true;
    }
    // 方法2: 通过角色和内容检查
    if (msg.role === role && msg.content && msg.content.trim() === normalizedContent) {
        return true;
    }
    return false;
});
```

### 消息结构
```javascript
{
    id: "a1b2c3d4e5f6g7h8",  // 唯一标识
    role: "user",            // 角色：user/assistant
    content: "消息内容",      // 消息内容
    timestamp: 1234567890    // 时间戳
}
```

### Hash算法
使用简单的DJB2 hash算法变体：
```javascript
function _generateMessageId(role, content) {
    let hash = 0;
    const str = `${role}:${content}`;
    for (let i = 0; i < str.length; i++) {
        const char = str.charCodeAt(i);
        hash = ((hash << 5) - hash) + char;
        hash = hash & hash; // Convert to 32bit integer
    }
    return Math.abs(hash).toString(16).substring(0, 16);
}
```

### 游戏上下文注入
在每次对话时，将最近玩的游戏信息注入到系统提示中：
```javascript
const lastGame = window.STORAGE.getLastGame?.() || { game: null, result: null };
const lastGameInfo = lastGame.game 
  ? `最近玩的游戏：${lastGame.game === 'gomoku' ? '五子棋' : '跳棋'}，结果：${lastGame.result === 'player' ? '主人赢' : lastGame.result === 'ai' ? '你赢' : '平局'}。`
  : '';
```

## 预期效果

### 修复前的问题
- ❌ AI会收到重复的历史记录
- ❌ AI会基于重复记录做出错误判断
- ❌ 角色会重复说相同的话
- ❌ 记忆混乱，无法正确理解上下文
- ❌ 记不住玩的是跳棋还是五子棋
- ❌ 下完棋后反复触发关于棋局的内心戏
- ❌ 自动说话过于频繁，每10-30秒就有动作
- ❌ 饥饿提醒过于频繁，每5-10分钟触发一次
- ❌ 喂食后AI回复包含"喂"字会再次触发喂食，形成无限循环

### 修复后的效果
- ✅ 自动检测并跳过重复消息
- ✅ AI只收到有效的历史记录
- ✅ 角色记忆清晰，不会重复
- ✅ 更好的上下文理解能力
- ✅ 详细的日志记录，便于调试
- ✅ 准确记住最近玩的游戏类型和结果
- ✅ 能正确回应关于游戏的对话
- ✅ 内心戏主题多样化，不再过度关注游戏
- ✅ 游戏信息随时间衰减，避免反复提及
- ✅ 自动行为频率降低，更加安静自然
- ✅ 25%概率保持安静，减少打扰
- ✅ 饥饿提醒间隔延长至5-15分钟
- ✅ 饥饿定时器正确读取配置文件
- ✅ 动作冷却机制防止喂食循环触发
- ✅ 所有动作类型都有5秒冷却保护
- ✅ AI角色身份明确，第一人称回复"我是寻慧"
- ✅ 移除生硬的"主人"称呼，改用更自然的"那个笨蛋"
- ✅ 保持傲娇毒舌的人格基调不变
- ✅ 历史记录使用第一人称，避免角色混淆
- ✅ 角色有独立人格，不卑微、不物化
- ✅ 角色有自己的生活，不24小时围着用户转
- ✅ 眼睛按钮始终保持在屏幕可见区域内
- ✅ 喂食系统调整为更符合独立人格，不再显得被动

## 日志输出示例

### 成功添加消息
```
[addToHistory] 成功添加消息: [user] 你好，今天天气怎么样？...
[addToHistory] 成功添加消息: [assistant] 哼，天气好不好关我什么事...
```

### 检测到重复
```
[addToHistory] 检测到重复消息，已跳过: [user] 你好，今天天气怎么样？...
[chat] 部分历史记录添加失败，可能存在重复
```

### 跳过空内容
```
[addToHistory] 跳过空内容
```

## 性能影响

- **内存占用**：略微增加（每条消息多存储id和timestamp）
- **CPU开销**：可忽略（hash计算非常快）
- **存储空间**：略微增加（JSON文件稍大）
- **整体影响**：正面，避免了重复数据累积

## 后续建议

1. **定期清理历史记录**
   - 可以添加自动清理机制
   - 保留最近N天的记录
   - 或根据重要性分级存储

2. **历史记录压缩**
   - 对于过长的历史记录进行摘要
   - 减少发送给AI的数据量
   - 提高响应速度

3. **重要消息标记**
   - 标记重要的对话
   - 长期保留关键信息
   - 优化记忆管理策略

## 修改文件列表

1. `src/renderer/storage.js` - 历史记录管理核心逻辑、游戏状态记忆
2. `src/renderer/chat.js` - 对话处理逻辑、游戏上下文注入、内心戏多样化
3. `src/renderer/index.js` - 启动逻辑
4. `src/renderer/config.js` - 状态配置（随机玩耍、饥饿间隔）
5. `src/renderer/state.js` - 随机行为概率调整
6. `src/renderer/drag.js` - 饥饿定时器配置读取

## 测试建议

### 测试场景
1. **重复消息测试**
   - 快速连续发送相同消息
   - 检查是否被正确去重

2. **历史记录加载测试**
   - 重启应用
   - 检查历史记录是否正确加载
   - 验证消息ID和时间戳

3. **长期使用测试**
   - 进行多轮对话
   - 检查AI是否还会重复
   - 验证记忆是否清晰

### 验证方法
- 查看控制台日志
- 检查历史记录文件（~/.girlpet_history.json）
- 观察AI回复是否正常

---

**修复日期**: 2026-05-20  
**修复版本**: v2.x  
**修复人员**: AI Assistant
