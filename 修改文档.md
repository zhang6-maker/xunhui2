# 寻慧 v2 改造文档

---

## 📋 目录

1. [日记模块改造](#1-日记模块改造)
2. [性别称呼修复](#2-性别称呼修复)
3. [安全加固措施](#3-安全加固措施)

---

## 1. 日记模块改造

### 1.1 问题分析

| 问题 | 描述 |
|------|------|
| ❌ 提示词污染 | 日记里出现发给AI的长指令 |
| ❌ 流水账日记 | 每次互动都写日记，刷屏且刻意 |
| ❌ 缺乏情感 | 硬编码模板，没有人味 |

### 1.2 改造方案：每日摘要模式

**核心思路**：停止实时写日记，改为记录事件到数组，每天固定时间（睡觉前）生成一篇AI总结日记。

### 1.3 修改文件清单

#### 文件1：`src/renderer/storage.js`

新增函数：

```javascript
// 当日事件数组
let todayEvents = [];

// 添加事件
function addEventForDiary(eventType, detail = '') {
    todayEvents.push({ type: eventType, detail, timestamp: Date.now() });
}

// 生成每日总结（AI生成）
async function generateDailySummary() {
    const events = getTodayEvents();
    if (events.length === 0) return;
    
    const prompt = `你是寻慧，一个傲娇毒舌的桌面精灵。
时间：${new Date().toLocaleDateString('zh-CN')}
今天发生的事：
${events.map(e => `- ${e.detail}`).join('\n')}

请用第一人称写一篇80-150字的日记，傲娇地总结今天。`;

    const diary = await callOllamaSilent(prompt);
    if (diary) writeDiary(diary);
    clearTodayEvents();
}
```

#### 文件2：`src/renderer/actions.js`

将所有 `autoDiary` 调用替换为 `addEventForDiary`：

| 原代码 | 新代码 |
|--------|--------|
| `autoDiary('feed')` | `addEventForDiary('feed', '吃了一份食物')` |
| `autoDiary('play')` | `addEventForDiary('play', '玩耍了一会儿')` |
| `autoDiary('search', keyword)` | `addEventForDiary('search', '搜索了 "xxx"')` |
| `talkToOllama(prompt, {skipLearning, skipIntercept})` | 添加 `skipDiary: true` |

#### 文件3：`src/renderer/chat.js`

- 将所有 `autoDiary('talk', userMessage)` 替换为 `addEventForDiary('talk', userMessage)`
- 用户名字设置改用 `addEventForDiary('profile', ...)`

#### 文件4：`src/renderer/state.js`

在进入睡眠状态时触发每日总结：

```javascript
// 到达床位
setState('sleeping');
window.STORAGE?.addEventForDiary('sleep', '睡觉了');
window.STORAGE?.generateDailySummary?.();  // 生成每日总结
```

### 1.4 改造效果

| 场景 | 原效果 | 新效果 |
|------|--------|--------|
| 喂食 | 每次喂食写一行 | 记录事件，晚上统一总结 |
| 对话 | 每次对话写日记 | 记录事件，晚上统一总结 |
| 每日查看日记 | 几十条流水账 | 一篇生动的AI总结 |
| 提示词问题 | 提示词写入日记 | ✅ 已阻止 |

---

## 2. 性别称呼修复

### 2.1 问题分析

用户反馈角色知道性别但仍使用不符的称呼（如女性用户被叫"老哥"）。

### 2.2 修复方案

增强 `chat.js` 中的 `genderHint` 指令强度：

**修改前**：
```javascript
genderHint = '【事实】用户是女性，这是确定的。你必须称呼用户为"姐姐"...';
```

**修改后**：
```javascript
genderHint = `【最重要】用户性别是女性。称呼规则：必须叫"姐姐"或"喂"或"笨蛋姐姐"。绝对禁止说"哥哥"、"老哥"、"男士"等任何男性称呼，一旦违规用户会生气。`;
```

同时将 `genderHint` 移到提示词最前面，确保AI优先处理。

### 2.3 修改位置

`src/renderer/chat.js` 第 298-302 行和第 335-337 行

---

## 3. 安全加固措施

### 3.1 Web安全设置

```javascript
webPreferences: {
    nodeIntegration: false,   // 禁止渲染进程访问Node.js
    contextIsolation: true,   // 隔离主进程和渲染进程
    webSecurity: true         // 启用Web安全策略
}
```

### 3.2 IPC通道白名单

```javascript
const ALLOWED_INVOKE_CHANNELS = new Set([
    'get-templates',
    'save-template',
    'select-ppt-file'
    // 只添加需要的通道
]);
```

### 3.3 路径遍历防护

```javascript
const ALLOWED_BASE_DIRS = [os.homedir(), os.tmpdir()];

function isPathAllowed(filePath) {
    const resolved = path.resolve(filePath);
    return ALLOWED_BASE_DIRS.some(base => resolved.startsWith(base));
}
```

### 3.4 命令注入防护

```javascript
// ❌ 不安全
exec(`python script.py ${userInput}`);

// ✅ 安全
spawn('python', ['script.py', userInput]);
```

### 3.5 内容安全策略（CSP）

```html
<meta http-equiv="Content-Security-Policy" content="default-src 'self'; img-src 'self' app: data:; script-src 'self'; style-src 'self'">
```

---

## 📁 修改文件汇总

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `src/renderer/storage.js` | 新增 | 事件记录系统、每日总结生成 |
| `src/renderer/actions.js` | 修改 | 替换日记调用方式 |
| `src/renderer/chat.js` | 修改 | 性别提示词增强、日记调用替换 |
| `src/renderer/state.js` | 修改 | 触发每日总结 |
| `FIX_GUIDE.md` | 更新 | 安全加固指南 |

---

## ✅ 改造验证清单

- [x] 日记只在睡觉时生成一篇总结
- [x] AI生成的日记带情感和细节
- [x] 提示词不再写入日记
- [x] 角色正确使用性别称呼
- [x] 安全加固措施已实施
- [x] 原有功能不受影响

---

*文档版本：v1.0*  
*最后更新：2026年5月28日*