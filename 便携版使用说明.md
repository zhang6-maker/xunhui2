# 寻慧桌面小精灵 · U 盘便携版

从 `E:\GirlPet_Clean\GirlPetMini\v2-1-beifen` 打包而成，**原版目录一个字节都没动**，两份互不影响。

- 便携包体积：**约 9.6 GB**（含内置 Ollama 运行时 + Qwen2 7B 模型）
- 打包日期：2026-09-07

---

## 一、怎么用

1. 把整个 `GirlPet-Portable` 文件夹拷到 U 盘（建议放 U 盘根目录）。
2. 在任意 Windows 电脑上双击 **`启动寻慧.bat`**（中文提示）或 **`Launch.bat`**（英文提示），两个功能完全一样，任选其一。
3. 首次启动会弹出黑色命令行窗口做环境自检，几秒后自动消失，随后寻慧窗口出现。

> 如果中文那个出现乱码，用 `Launch.bat`——它是纯英文的，在任何语言版本的 Windows 上都不会乱码。
> 文件夹里还有个原版自带的 `start.bat`，它也能启动，但**不会做数据重定向**，数据会写到那台电脑的 C 盘，别用它。

不需要安装、不需要 Node.js、不需要 Python——全部内置。

> 建议 U 盘用 **NTFS 或 exFAT** 格式（FAT32 单文件上限 4GB，虽然本包没有超 4GB 的单文件，但 exFAT 更省心）。
> 建议插 **USB 3.0** 口：2.7GB 的应用从 U 盘加载，USB 2.0 会很慢。

---

## 二、数据存在哪（便携的关键）

所有个人数据都写在便携包自己的 **`UserData\`** 目录里：

| 目录 | 内容 |
|---|---|
| `UserData\Home` | 记忆、聊天记录、日记、好感度、提醒、设置（`.girlpet_*` 文件） |
| `UserData\ElectronProfile` | 浏览器层数据（localStorage、缓存等） |
| `UserData\Roaming` / `Local` | 应用运行期数据 |
| `UserData\Home\Desktop` | 生成的 PPT 文件（原本保存在电脑桌面，现已改到这里） |

**换电脑时数据跟着 U 盘走，用过的电脑上不会留下任何痕迹。**
（已实测：运行便携版后，C 盘 `C:\Users\xxx\` 下的 `.girlpet_*` 文件时间戳没有任何变化。）

想要"全新开始"，把 `UserData` 整个文件夹删掉即可，下次启动会自动重建。

---

## 三、哪些功能能用，哪些要靠外部环境

| 功能 | 状态 | 说明 |
|---|---|---|
| 桌宠显示、互动、好感度、提醒、日记、小游戏 | ✅ 自带 | 纯本地，不依赖外部 |
| 中文语音识别 | ✅ 自带 | 内置 2GB vosk 中文模型 + 便携 Python |
| 语音朗读（TTS） | ⚠️ 需联网 | 内置便携 Python 与 edge-tts，但要能访问微软的在线语音服务 |
| PPT 生成 | ✅ 自带 | 生成的 `.pptx` 保存在 `UserData\Home\Desktop` |
| **AI 对话（大模型）** | ✅ 已内置 | 包内自带 Ollama + `qwen2:7b`，**完全隔离在 `127.0.0.1:11435`**，离线可用，不依赖目标机装 Ollama |

关于 AI 对话：便携版已自带推理引擎与模型，双击启动器即离线可用。寻慧只连接包内这份 11435 的 Ollama，**绝不连宿主 11434、不读宿主模型库**。若需要换模型/地址，仍可在寻慧设置面板里改 Ollama 地址（例如指向局域网或云端）。

---

## 四、已知小毛病（原版就这样，没改）

- **会自动弹出开发者工具窗口**：`main.js` 第 53 行有 `win.webContents.openDevTools()`。嫌它碍眼的话，用记事本打开 `main.js`，在第 53 行行首加两个斜杠 `//` 注释掉即可。
- **端口固定**：HTTP 用 8080、WebSocket 用 8083。如果目标电脑上这两个端口被别的软件占了，手机遥控那类功能会失效（桌宠本身不受影响）。
- **透明窗口依赖显卡合成**：极老的机器或用远程桌面时，可能出现黑底。正常台式机/笔记本没问题。

---

## 五、目录说明

```
GirlPet-Portable\
├─ 启动寻慧.bat        ← 双击这个（中文提示）
├─ Launch.bat          ← 或双击这个（英文提示，功能完全相同）
├─ main.js / index.html / preload.js / src\ / libs\ / images\   ← 应用本体
├─ node_modules\       ← 469MB，含 Electron 运行时（已剔除开发期依赖）
├─ python_portable\    ← 214MB，Python 3.11.9 便携版（vosk / edge-tts 等已装好）
├─ bin\                ← 2GB，中文语音识别模型
└─ UserData\           ← 你的全部数据，跟着 U 盘走
```

瘦身说明：`node_modules` 从 780MB 精简到 469MB（删掉了 electron-builder、jest、husky 这类只在开发打包时用到、运行期不需要的依赖），功能不受影响，已实测启动正常。

---

## 六、这次打包做了什么改动

只加了两个启动器和一个**独立的传话信箱模块**（见第八节），**没有修改任何一行业务逻辑**。启动器做三件事：

1. 用 `%~dp0` 定位自身目录——U 盘盘符从 E: 变成 F:、G: 都不受影响。
2. 把 `USERPROFILE` / `HOME` / `APPDATA` / `LOCALAPPDATA` 重定向到便携包内的 `UserData\`，让原本写进 C 盘用户目录的数据改道到 U 盘。
3. 清掉 `ELECTRON_RUN_AS_NODE` 这个环境变量（某些电脑上有它，会导致 Electron 退化成纯 Node 而启动失败），并给 Electron 传 `--user-data-dir` 指向 U 盘。

已实测通过：应用正常启动，协议注册、WebSocket(8083)、HTTP(8080)、页面加载、渲染进程数据写入全部正常，且目标机器 C 盘无写入。

---

## 七、后续可选：把 AI 也塞进 U 盘（当前版本没做）

现在这版**不含大模型**，AI 对话依赖目标电脑自己装 Ollama。想做到"走到哪都能聊"，可以后期加装，容量完全够：

| 需要追加的东西 | 体积 | 说明 |
|---|---|---|
| Ollama 便携版 `ollama-windows-amd64.zip` | 约 1 GB | 官方提供的免安装 CLI 包，配 `OLLAMA_MODELS` 环境变量可把模型放在 U 盘 |
| 中文小模型（3B~4B，如 qwen2.5:3b） | 约 2~3 GB | 再大 CPU 就跑不动了 |

64G 盘装完这些总共约 6~7GB，仍然很空。

**加装前必须想清楚的三件事**（决定了再动手，别白下载）：

1. **Ollama 便携包按显卡分包**，一个 U 盘只能放一种：NVIDIA 版自带 CUDA，AMD 要 ROCm 版，Intel 核显又是一个包。放错版本会退回纯 CPU（能跑，但慢）。
2. **没独显就只能用 1B~3B 小模型**，纯 CPU 推理每秒几个字，能聊但明显卡；目标电脑内存低于 8G 会很痛苦。
3. **冷启动要从 U 盘读 2~3GB 模型**，廉价 U 盘可能等一两分钟；`ollama.exe` 还可能被 360 之类拦截。

（这部分已经做进便携版了：启动器里设了 `OLLAMA_MODELS` / `OLLAMA_HOME` / `OLLAMA_HOST=11435`，后台 `start /min ollama serve` 拉起包内这份，不再需要你手动加。）

---

## 八、传话信箱（便携版新增功能）

寻慧和你之间多了一个**双向留言通道**：你在寻慧窗口里说话，消息落到 U 盘；WorkBuddy 读走、办完事再回你一条，寻慧会弹气泡提醒。

### 怎么用

1. 寻慧窗口右下角多了一个 **✉ 按钮**（眼睛按钮左边），有未读消息时右上角会显示红点数字。
2. 点击打开信箱面板，输入想让 WorkBuddy 做的事，**回车发送**（Shift+回车换行）。
3. WorkBuddy 的回复会出现在面板里，同时寻慧会冒气泡提示。

### 信箱在哪

```
UserData\Home\mailbox\
├─ outbox\   寻慧 -> WorkBuddy（*.json）
├─ inbox\    WorkBuddy -> 寻慧（*.json）
└─ sent\     已处理归档
```

消息是纯 JSON，长这样，人也能直接看懂和手改：

```json
{
  "id": "20260908-141051-s9hc",
  "from": "xunhui",
  "to": "workbuddy",
  "time": "2026-09-08T14:10:51",
  "kind": "user",
  "text": "帮我出一张吉卜力风格的图"
}
```

### 命令行工具（给 WorkBuddy / 你自己用）

`tools\mailbox.py` 会自动定位信箱，U 盘盘符变了也不用改路径：

```
python tools\mailbox.py list              列出 outbox 待处理消息
python tools\mailbox.py read <id>         看某条消息全文
python tools\mailbox.py reply "文本"       给寻慧回一条
python tools\mailbox.py done <id>         归档到 sent
python tools\mailbox.py list --folder inbox   看已发出去的
```

### 改了哪些文件

| 文件 | 改动 |
|---|---|
| `src\renderer\mailbox.js` | 新增，信箱 UI 与收发逻辑，自包含不碰其他模块 |
| `main.js` | 新增两个 IPC：`fs-readdir`（列目录，仍受原白名单限制）和 `mailbox-ensure`（建目录） |
| `preload.js` | 暴露 `fsReaddirSync` / `mailboxEnsure` 两个方法 |
| `index.html` | 多引一行 `mailbox.js` |
| `tools\mailbox.py` | 新增，命令行工具 |

安全边界没动：文件读写仍只允许 `homedir`（也就是 U 盘的 `UserData\Home`）和临时目录。

### 能跨设备吗

**能，但是"U 盘搬运式"的异步跨设备，不是实时网络同步。**

- 信箱文件就存在 U 盘里，跟着 U 盘走。在 A 电脑发的消息，拔下 U 盘插到 B 电脑，消息还在。
- 已读状态会**写回消息文件**（`"read": true`），所以换设备或重启后，旧消息不会重复弹，只会显示没读过的新消息。这点已实测：重启后旧消息 0 条重复、新消息正常送达。
- 我没法实时感知——**只有你唤起我、我去读信箱时才处理**。不在会话中时，消息会安静地躺在 outbox 里等你。
- 想让"自动收信"，可以建一个定时任务让我定期检查信箱（盘符变了也能扫到）。

**已实测**：出站（寻慧 → outbox）与入站（WorkBuddy → inbox → 寻慧弹消息）双向都通，数据全部落在 U 盘。

### 本地提醒（零积分，推荐）

WorkBuddy 的定时任务**会消耗积分**（配置存本地，但执行时要发起 Agent 任务）。如果只想"有信时知道一声"，用纯本地方案就够了——它不调用任何 AI，一个积分都不花：

| 脚本 | 作用 |
|---|---|
| `tools\安装本地信箱提醒.bat` | 双击，注册一个 Windows 计划任务，每 2 小时检查一次信箱，有新消息就弹窗 |
| `tools\立即检查信箱.bat` | 双击，立刻看一眼有没有新消息（不弹窗，只打印） |
| `tools\卸载本地信箱提醒.bat` | 双击，删掉计划任务，停止提醒 |
| `tools\mailbox_notify.py` | 被上面调用的脚本本体，`--dry-run` 只看不弹、`--reset` 清空已提醒记录 |

特点：自动扫盘找信箱（U 盘换盘符不用改配置）、用 `pythonw` 运行不闪黑窗、提醒过的消息不会重复弹。

看到弹窗后，再来找 WorkBuddy 处理即可——**只有真正要干活时才花积分**。

---

## 九、手机遥控（局域网 + 可"添加到主屏幕"）

让手机连上同一 WiFi，远程看寻慧、发消息聊天、让她工作/睡觉/玩耍、下五子棋跳棋。**只走局域网，不出公网**，所以不用内网穿透、不用账号、不用花积分。

### 前置条件

- 手机和电脑在**同一个 WiFi（同一局域网）**。
- 电脑的**防火墙放行 8080、8083 端口**（Windows 默认会拦，见下）。
- 想用"聊天/AI 互动"的话，电脑本机得有 Ollama 在跑（见第三节）；纯遥控、下棋不需要 AI。

### 怎么连

**最省事（推荐）**：寻慧窗口右下角点 **📱 按钮**（和 ✉ 信箱按钮并排），会弹出一个面板，里面是实时生成的连接二维码——手机相机/扫一扫扫一下直接进遥控页，**不用自己查 IP、不用手敲地址**。IP 变了（DHCP 续租）重新点一下按钮即可，二维码跟着刷新。面板里也有「复制链接」备用。

1. 在电脑上启动寻慧（双击 `启动寻慧.bat`）。
2. 在手机浏览器地址栏输入：**`http://<电脑局域网IP>:8080/mobile`**
   - 最省事：直接点寻慧窗口的 📱 按钮扫二维码，地址自动给对，不用手查。
   - 电脑局域网 IP 怎么看：电脑上 `ipconfig` 找**物理网卡**（"WLAN"/"无线局域网"/"以太网"）的"IPv4 地址"（一般是 `192.168.x.x` 或 `10.x.x.x`）。
   - ⚠️ **千万别用虚拟网卡地址**：VirtualBox Host-Only（常见 `192.168.56.1`）、VMware VMnet8 / VMnet1（`192.168.x.x`）是虚拟机专用网段，手机不在里面，用了会**连接超时（浏览器报错 `错误代码 -7` / `TIMED_OUT`）**。旧版 📱 按钮曾误显示这种地址，现已修复——请**重启寻慧**后，按钮弹出的就是正确的物理网卡 IP。
3. 页面显示"✅ 已连接到电脑"即可用。按钮：🍎喂食 / 💻工作 / 😴睡觉 / 🎈玩耍 / 🎲五子棋 / 🎲跳棋；下方输入框可直接和寻慧对话，她的回复会实时回显在手机上。

> 每次寻慧启动都会随机生成一个访问令牌，手机连上时自动带上，断线会自动重连。令牌只在局域网内有效，别人连同一 WiFi 也能进，介意的话别在公共 WiFi 上开。

### 装成手机 App（PWA）

不用下 APK、不用应用商店：

- **iPhone / iPad（Safari）**：打开上面的地址 → 点底部"分享"按钮 → "添加到主屏幕" → 取名"寻慧遥控"。以后从桌面图标进，全屏无地址栏，像原生 App。
- **安卓（Chrome）**：打开地址 → 点右上菜单"⋮" → "添加 to 主屏幕"（或"安装应用"）。少数安卓版本要求 HTTPS 才弹"安装"，那种情况会降级成"加到主屏幕"的书签快捷方式，功能一样。

已内置 `manifest.webmanifest` + `sw.js` + 图标，离线也能打开外壳。

### 防火墙放行（重要，否则手机连不上）

Windows 防火墙默认拦 8080/8083，手机打不开页面、连不上遥控。二选一：

**方法 A（推荐，命令行一次性放行）**——在电脑上以管理员打开 PowerShell，执行：

```powershell
New-NetFirewallRule -DisplayName "寻慧手机遥控-HTTP" -Direction Inbound -LocalPort 8083,8080 -Protocol TCP -Action Allow
```

**方法 B（图形界面）**——控制面板 → Windows Defender 防火墙 → 高级设置 → 入站规则 → 新建规则 → 端口 → TCP → 特定本地端口填 `8080,8083` → 允许连接 → 全勾 → 命名"寻慧手机遥控"。

> 如果公司/校园网禁设备互访，手机可能连不上——那是网络策略，换家里 WiFi 或开手机热点给电脑连即可。

### 一键放行（最省事）

包里提供了 `放行防火墙.bat`：**右键该文件 → 以管理员身份运行**，即可自动添加 8080/8083 的入站放行规则。普通双击会因权限不足而 `Access is denied`，必须用管理员身份运行。

### 连不上的典型错误（对照排查）

| 现象 | 根因 | 解决 |
|---|---|---|
| 浏览器报 **`错误代码 -7` / `TIMED_OUT`**，页面打不开 | ① 地址用了虚拟网卡 IP（如 `192.168.56.1`）；② 防火墙没放行 | 用 📱 按钮显示的**物理网卡 IP**（重启寻慧后更新）；运行 `放行防火墙.bat` |
| 页面能开，但一直"连接中"转圈 | WebSocket 8083 被拦，或令牌过期 | 放行 8083；重启寻慧重新生成令牌再扫 |
| 能连上但 AI 不说话 | 内置 Ollama 没拉起，或模型名不对 | 见第十节；确认 `启动寻慧.bat` 已拉起包内 Ollama |
| 电脑本机 `localhost:8080/mobile` 能开，手机不行 | 手机与电脑不在同一 WiFi，或防火墙拦截 | 确认同网段；运行 `放行防火墙.bat` |

### 改了哪些文件（相对上一版）

| 文件 | 改动 |
|---|---|
| `src\main\websocketServer.js` | WebSocket 改监听 `0.0.0.0`（允许局域网），并校验 `?token=`，兼容手机发的 `action` 字段 |
| `main.js` | 启动时生成令牌并下发给 WebSocket；新增 `/ip` 接口返回局域网 IP+令牌；渲染进程回复经 `reply-to-mobile` 广播回手机；HTTP 服务改监听 `0.0.0.0` 并托管 PWA 静态文件 |
| `src\renderer\chat.js` | 收到 `mobile-command` 后映射成对应动作（喂食/工作/睡觉/玩耍/五子棋/跳棋/对话），并把寻慧的回复回传给手机 |
| `preload.js` | 放行 `reply-to-mobile`（发）与 `mobile-command`（收）两个通道 |
| `mobile.html` | 先拉 `/ip` 拿 IP+令牌再连 WebSocket；加 PWA meta、manifest 引用、Service Worker 注册 |
| `manifest.webmanifest` / `sw.js` / `icon-192.png` / `icon-512.png` | 新增，PWA 安装所需 |

### 已知限制

- **仅局域网**：出门在外想遥控，需要内网穿透（如 Tailscale）把 8083/8080 映射出去——当前版本没做，需要再说。
- 同一时刻所有连上的手机看到的是同一只寻慧（广播式，不是一对一私聊）。
- 端口固定 8080/8083，被占用则遥控失效（桌宠本体不受影响）。

---

## 十、内置 AI（Ollama + Qwen2 7B）

便携包现在**自带 Ollama 推理引擎和 Qwen2 7B 模型**，寻慧的 AI 对话可以完全离线跑，不用目标电脑自己装 Ollama、也不用联网下模型。

- `Ollama\` —— Ollama 运行时（含 CUDA 库，自带依赖，不依赖系统 VC++）。
- `OllamaHome\models\` —— `qwen2:7b` 模型（Q4_0 量化，约 4.2 GB），正是寻慧代码里默认的模型名。

### 它是怎么起来的（完全隔离，不碰宿主 Ollama）

`启动寻慧.bat` / `Launch.bat` 会设置三个环境变量，把包内这份 Ollama 钉死在独立端口、独立模型库：

- `OLLAMA_HOME=<包>\OllamaHome` —— 运行时数据（日志/缓存）留在 U 盘；
- `OLLAMA_MODELS=<包>\OllamaHome\models` —— **显式覆盖**模型目录，只认包里这份；
- `OLLAMA_HOST=127.0.0.1:11435` —— 专用端口，与宿主 Ollama 默认的 11434 错开。

然后**无条件**用包里的 `Ollama\ollama.exe serve` 在 **11435** 上拉起本地推理服务（最小化窗口），等待最多 30 秒就绪再启动寻慧。整套链路**从不连接宿主 11434、也不读宿主模型库**——这是关键：你机器上若全局设了 `OLLAMA_MODELS`（比如指向 D 盘某个大库），bat 里这行 `set OLLAMA_MODELS=...` 会把它盖掉，强制只用 U 盘里这份。

所以：

- **插到没装 Ollama 的电脑** → 包里这份自己 serve，离线可用；
- **插回你自己的笔记本（你那台 Ollama 正跑在 11434）** → 两份并存、互不干扰，寻慧只认 11435 这份；
- **之前担心的边界情况（目标机有 Ollama 却没 qwen2:7b）** 已彻底消失——因为寻慧根本不看宿主那一份。

### 前置 / 性能

- 7B-Q4 大概需要 **6 GB 显存或内存**才能跑得动。
- 有 NVIDIA 独显（如你的 RTX 4060 8GB）→ 走 CUDA，速度很快（~20 tok/s 量级）。
- 只有核显/无独显 → 走 CPU 推理，会明显变慢，内存不够可能直接跑不起来。
- 模型是 Q4_0 量化，效果和精度比满血版略低，但本地聊天够用。

### 数据安全

模型与运行时都写在包内 `OllamaHome\`，个人聊天数据仍在 `UserData\`，换电脑不留痕。唯一的极小运行时缓存也在 `OllamaHome\` 里，主机 `C:\Users\xxx` 不会被写。

> 原版 `E:\GirlPet_Clean\...` 一个字节没动；这 9.6GB 是新增的内置 AI，不是覆盖原版。
